const mongoose = reuqire('mongoose');
const Schema = mongoose.Schema

const JourneySchema = new Schema({
  name: {
    type: String,
    requried: true
  },
  name: {
    type: String,
    requried: true
  }
  name: {
        type: String,
        requried: true
    },
});